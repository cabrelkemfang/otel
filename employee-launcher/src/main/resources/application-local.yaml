spring:
  datasource:
    url: jdbc:mysql://localhost:3306/otel_employee_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true&createDatabaseIfNotExist=true
    username: root
    password: mysql

  jpa:
    show-sql: true

logging:
  level:
    io.growtogether: DEBUG
    org.springframework.web: DEBUG
